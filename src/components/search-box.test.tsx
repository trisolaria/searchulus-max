import { describe, test } from "vitest";

/**
 * A test API is set up with MSW, allowing normal `fetch` requests to be intercepted
 * Use can use `fetch` as you would in any component.
 */
describe("SearchBox", () => {
  test("should work", async () => {
    // Tests go here
  });
});
